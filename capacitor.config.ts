import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shapeshifter.app',
  appName: 'ShapeShifter',
  webDir: 'dist/shapeshifter999',
  bundledWebRuntime: false
};

export default config;
